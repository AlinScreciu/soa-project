kind: Secret
apiVersion: v1
metadata:
  name: rabbitmq-cluster-deployment-default-user
stringData:
  default_user.conf: |
    default_user = user
    default_pass = password
  password: "password"
  username: "user"
type: Opaque